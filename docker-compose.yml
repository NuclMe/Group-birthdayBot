services:
  mongodb:
    image: mongo:8
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - mongodb

volumes:
  mongodb_data:
